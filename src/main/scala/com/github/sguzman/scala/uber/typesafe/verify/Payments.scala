package com.github.sguzman.scala.uber.typesafe.verify

case class Payments(
                   allowChildMatching: Boolean,
                   name: String,
                   nav: EarningsNav,
                   primarySideNav: Int
                   )
